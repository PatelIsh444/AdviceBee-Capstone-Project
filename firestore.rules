rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
  
    match /{document=**} {
      allow read;
      allow write: if request.auth != null;
    }

  
  // TODO: Investigate why this keep breaking the DB read/writes
  

//     // TODO
//     // - This collection needs re-work. Allowing read/write for now.
//     match /Notification/{uid} {
//     	allow read;
//       allow write: if request.auth != null;
//     }
    
//     // TODO: 
//     // - Restrict creation to queen bees only
//     // - Restrict modification unless user is apart of group
//     match /groups/{groupId} {
//     	allow read;
//       allow write: if request.auth != null;
//     }
    
//     // TODO:
//     // - This collection needs re-work. Allowing read/write for now.
//     match /reports/{uid} {
//     	allow read;
//       allow write: if request.auth != null;
//     }
    
//     // TODO:
//     // - Clarify who can create topics. Can anyone create a general topic or are they "hardcoded"?
//     match /topics/{topic} {
//     	allow read;
//       allow write: if request.auth != null;
//     }
    
//     match /users/{uid} {
//     	allow read;
//       allow write: if request.auth.uid == uid;
//     }
  }
}